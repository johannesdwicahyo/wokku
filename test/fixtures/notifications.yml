one:
  app_record: one
  team: one
  channel: 0
  events: '["deploy.succeeded", "deploy.failed"]'
  config: '{"notify_on_failure": true}'

two:
  team: two
  channel: 1
  events: '["deploy.succeeded"]'
  config: '{}'

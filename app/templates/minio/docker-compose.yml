# documentation: https://github.com/minio/minio
# slogan: S3-compatible object storage
# category: storage
# tags: s3, object-storage, go
# icon: storage
# port: 9000

services:
  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"

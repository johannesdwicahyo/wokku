# name: Django
# documentation: https://github.com/django/django
# slogan: Python web framework for perfectionists with deadlines
# category: frameworks
# tags: python, django, fullstack
# icon: python
# post_deploy: python manage.py migrate

services:
  django:
    image: django:latest
    depends_on:
      - postgres

  postgres:
    image: postgres:16
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
